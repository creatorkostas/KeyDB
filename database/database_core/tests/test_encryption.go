package database_test

import (
	"testing"
)

func TestRemoveThread(t *testing.T) {
	// var acc, priv_key, err = cmd_api.Register("kostas", "Admin", "test", "test")

	// if acc == nil || priv_key == "" || err != nil {
	// 	t.Fatalf("something went wrong")
	// }

	// var ok, err_set = cmd_api.SetValues(acc.Api_key, acc, "int", "2")

	// if err_set != nil || !ok {
	// 	t.Fatalf("something went wrong")
	// }

}
